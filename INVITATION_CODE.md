# Invitation Code

The current invitation code for user registration is:

**`GENIE2024`**

> Note: This code is currently hardcoded in the backend (`AuthController.java`).
> Future updates may move this to a database or configuration file.
